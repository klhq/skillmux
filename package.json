{
  "name": "skill-router",
  "version": "0.1.0",
  "description": "Local read-only MCP server routing task queries to skills in ~/.agents/skills with zero-loss SKILL.md delivery",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "index": "bun run src/cli.ts index",
    "eval": "bun run src/cli.ts eval"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
