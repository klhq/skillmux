# Minimal battery-included setup. This entire file is optional: these are the
# built-in defaults. Copy it only when your skill vault is elsewhere.
vault_path = "~/.agents/skills"

[inference]
mode = "local"
